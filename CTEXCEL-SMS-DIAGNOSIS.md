# CTExcel / SIM7600CE-T 短信故障诊断记录

最后更新：2026-08-07

## 2026-08-07 QDC507 结果更新

本文原有结论仍准确解释 SIM7600CE-T 的失败，但不再代表当前 DJI 工作区状态。替换为大疆 4G 模块一代（QDC507）后，在保持 `ROW_Generic_3GPP`、MBN AutoSel=1 的情况下强制 IMS 并重启，`AT+QCFG="ims"` 最终达到 `1,1`。随后用户触发的 CTExcel 短信进入 `ME` 存储，AT 侧出现 `+CMTI`/计数新增，`CMGR` 返回 DCS=8 的有效 UCS2 正文。

因此新的工程结论是：CTExcel 在 QDC507 上的入站和出站 SMS over IMS 均可行；生产实例向 `888` 发送 1 次 `BAL` 已取得真实 `+CMGS`，并收到、合并官方回复后自动更新余额。原 SIM7600 的根因确实是缺少可用 IMS 短信通道。QDC 驱动、MBN 失败回退、IMS 配置和隐私安全验收详见 [DJI-QDC507-CTEXCEL.md](DJI-QDC507-CTEXCEL.md)。

## 历史结论（SIM7600CE-T，2026-08-05）

现场 A/B 测试已经把故障范围基本锁定：CTExcel SIM 在手机中同样漫游到中国电信，关闭 Wi-Fi、关闭 CTExcel 数据漫游且不使用普通移动数据时仍能收短信；关闭手机的 VoLTE 后验证码不再送达，重新开启 VoLTE 并等待 IMS 重新注册后，之前滞留的短信延迟到达。结合 SIM7600CE-T 的 `CEREG=5`、`CGREG=5`、`CREG=3` 以及 AT 串口完全没有 `+CMTI/+CMT`、出站没有 `+CMGS`，可基本确认：**CTExcel 在当前中国电信漫游环境中的短信依赖 IMS/VoLTE 承载，而这版 SIM7600CE-T 固件/运营商 profile 没有建立可用的 IMS 短信通道。**

因此本次根因不在 Web、Telegram、短信解析、本地存档、普通 Internet APN、移动数据开关或信号强度。严格来说，尚未通过运营商侧信令或厂商命令直接读取 IMS 注册状态，所以“SMS over IMS”仍属于由决定性现场现象支持的工程结论，而不是 CTExcel/SIMCom 的正式书面确认。

## 使用边界

- 本文记录的是现场诊断状态，不替代 `SPEC.md` 至 `SPEC-V5.md` 的产品约束。
- 运行时状态会变化，继续操作前必须重新查询；COM11 只是 2026-08-05 的实测端口，代码仍须动态探测。
- 不记录或输出电话号码、IMSI、IMEI、Telegram token、`chat_id`、验证码或短信正文。
- 不执行 `NETOPEN`、`CGACT` 等数据业务指令，不启用 WWAN。
- 不自动重复发送 `BAL` 或测试短信。任何出站短信都可能收费，必须由用户明确确认。
- 不盲刷固件；升级前必须取得匹配型号的固件、校验值、升级说明和回退方案。

## 可复现判据

一次有效的入站测试应当这样判断：

1. 软件已连接模块，先记录 `/api/messages` 的数量、最新 `epoch` 和 `archive.jsonl` 修改时间。
2. 用户从 CTExcel 官网触发一次验证码；代理只观察，不自行发短信。
3. 最多等待 2 分钟。
4. 以下任一事件出现即说明底层入站链路已经恢复：
   - AT 口出现 `+CMTI`；
   - AT 口出现直接交付的 `+CMT`；
   - `/api/messages` 出现更新的 `epoch`；
   - `archive.jsonl` 新增记录。
5. Telegram 推送属于下游验证。只有本地存档已经新增、Telegram 仍未推送时，才进入 Telegram 排查。

SIM7600CE-T 当时的失败表现是：用户触发验证码后，上述四个底层/本地事件均未出现。QDC507 已在 2026-08-07 满足 AT 事件/存储新增判据。

## 2026-08-05 现场状态

| 项目 | 现场值或结果 | 含义 |
|---|---|---|
| 模块 | SIM7600CE-T | 中国市场 SIM7600CE 系列变体 |
| 固件 | `LE20B04SIM7600M22_221130` | 未找到该精确版本的公开发布说明 |
| AT 口 | COM11（仅本次实测） | 软件连接正常，端口不可写死 |
| 驻网 | `CHN-CT CTExcel` | 自动选网后驻留中国电信 |
| 注册域 | `CEREG=5`、`CGREG=5`、最终 `CREG=3` | EPS/分组域漫游成功；电路域注册被拒绝 |
| 信号 | 接好 LTE 天线后约 29 | 天线带来明显改善，弱信号已不是首要假设 |
| WWAN | `absent` | 当前没有可用 WWAN 数据网卡，符合零流量约束 |
| SMS 设置 | `CNMI=2,1,0,0,0`、`CSMS=0,1,1,1`、`CGSMS=2` | AT 短信设置存在，但不证明网络提供短信承载 |
| 手机访问网 | 中国电信，状态栏显示 `R` | 与模块驻留同一家中国运营商，排除“手机走移动/联通而模块走电信”的差异 |
| 手机普通数据 | Wi-Fi 关闭、CTExcel 数据漫游关闭，用户确认未使用普通移动数据 | 短信不依赖可见的 `everywhere` Internet APN 或普通上网流量 |
| 手机 VoLTE A/B | 关闭 VoLTE 后收不到；重新开启并等待后短信延迟到达 | 现场基本确认短信依赖 IMS/VoLTE 通道；延迟符合重新注册后补投递 |

“已注册”只表示至少一个网络注册域成功，不表示短信已经可用。短信可用必须由入站 `+CMTI/+CMT` 或出站 `+CMGS` 单独验证；手机端能通过 IMS 收取，也不等于通用模组已完成 IMS 注册。

## 已完成的排查

| 测试 | 结果 | 结论 |
|---|---|---|
| 同一张 CTExcel SIM 放入手机收发短信 | 正常 | SIM、账户和 CTExcel 服务本身正常；携号转网或账户异常不是模块失败的合理解释 |
| 手机驻网核对 | 手机明确显示中国电信并带漫游 `R` | 手机与模块都使用中国电信，排除访问网不同导致的假象 |
| 手机关闭 Wi-Fi、关闭 CTExcel 数据漫游且不使用普通移动数据 | 仍可接收短信 | `everywhere` 普通 Internet APN 和普通流量不是短信送达条件 |
| 手机关闭 VoLTE 后触发验证码 | 未收到 | 传统路径在该漫游组合下不可用，短信对 VoLTE/IMS 有实际依赖 |
| 手机重新开启 VoLTE | 等待注册后，先前短信延迟收到 | 与 IMS 重新注册后运营商补投递高度一致，是本轮最关键的决定性证据 |
| 软件等待 CTExcel 官网验证码 | 多轮均无新存档 | 前端、Telegram 之前的入站链路没有数据 |
| 直接抓取 AT 口，包含与应用一致的 DTR/RTS 状态 | 用户触发验证码后 `0 bytes` | 标准 `+CMTI/+CMT` 没有到达串口，解析器不是首要原因 |
| 向 `888` 发送 `BAL` | 等待超时，没有 `+CMGS` | 出站提交链路也未建立；不要无确认重复测试 |
| 手动尝试移动、联通网络 | 均返回 `+CME ERROR: no network service` | 当前 SIM/模块组合无法切换到这两个候选网络；之后已恢复自动选网 |
| 接好 LTE 天线后再次接收验证码 | 信号由约 15 提升到 29，但仍无新消息 | 天线确实改善射频条件，但没有修复短信承载 |

## 技术文档结论

本项目上级目录中的 [SIM7500/SIM7600 AT 手册](<../SIM7500_SIM7600 Series_AT Command Manual_V3.00(1).pdf>) 给出以下关键语义：

- 第 228–229 页：`AT+CGSMS=2` 表示“GPRS/分组域优先，失败后回落电路域”。它不是 IMS、VoLTE 或 LTE 短信的启用命令。
- 第 110–111 页：`CEREG=5` 只表示 EPS 漫游注册成功，不证明短信承载可用。
- 第 84–86 页：`CREG=3` 表示电路域注册被拒绝。
- 第 226–228 页：`CNMI` 决定 `+CMTI/+CMT` 的交付方式，并受 DTR 活跃状态影响；本轮已在 DTR/RTS 活跃条件下抓取，仍没有收到任何字节。
- `CSMS=0,1,1,1` 说明模块 AT 实现声称支持 MT/MO/广播短信类型，但不证明当前网络已经提供承载。
- `CSCA` 只影响移动发起短信的短信中心地址，无法解释入站串口完全没有事件。

SIMCom 官网列出的 V3《SMS Application Note》在 TEXT 模式说明中把 LTE 支持写为中国移动/中国联通，没有列中国电信；与此同时，[SIM7600CE 产品页](https://cn.simcom.com/product/SIM7600CE.html) 又列出 VoLTE、SMS 和中国电信认证。两者并不直接矛盾：产品族具备能力，不代表当前板卡固件、运营商 profile 和漫游 SIM 组合一定可用。

通用 AT 手册没有记录 `AT+CIREG`、`AT+CVLTE` 等 IMS/VoLTE 状态命令。因此模块对这些试探命令返回 `ERROR`，只能说明命令不受当前固件支持，不能单独证明模块完全没有 VoLTE。

## 开源社区对照

- [LilyGO issue #81](https://github.com/Xinyuan-LilyGO/T-SIM7600X/issues/81)：VoLTE-only SIM 用户同样遇到产品资料声称支持 VoLTE、但找不到可用启用命令的问题。该 issue 未得到确定解决，属于相似现象，不是本机根因证明。
- [LilyGO issue #126](https://github.com/Xinyuan-LilyGO/T-SIM7600X/issues/126)：相同 `no network service` 最终由 LTE 天线误接 AUX 而不是 MAIN 导致。本机接好天线后信号已达约 29 且 EPS 注册成功，因此完全相同的天线故障概率较低，但仍应确认天线物理连接在 MAIN。
- [LilyGO issue #63](https://github.com/Xinyuan-LilyGO/T-SIM7600X/issues/63)：存在 `+CMTI: ,-1`、短信未正确落库的公开案例。本机抓取结果是完全没有字节或 URC，症状不同。

没有找到“CTExcel + SIM7600CE-T + 中国电信漫游短信”或固件 `LE20B04SIM7600M22_221130` 的完全匹配公开问题。

## 排名后的根因假设

1. **极高：当前固件/运营商 profile 无法完成 CTExcel 所需的 IMS/VoLTE 短信注册。** 手机在同一中国电信漫游网络上关闭 VoLTE 即失去短信、重开后补投递；模块只有 EPS/分组域注册而无任何短信 AT 事件，证据链已经闭合。
2. **中：SIM7600CE 产品族具备 VoLTE 能力，但当前板卡固件裁剪、版本过旧或缺少适配的 MBN/QCN。** 需要 SIMCom 或板卡卖家确认，不能仅凭产品页写有“VoLTE/中国电信认证”推断当前国际漫游组合可用。
3. **低：天线或射频连接。** 接天线后信号显著改善并保持 LTE 注册，但短信仍失败；只剩确认 MAIN/AUX 接口的必要性。
4. **极低：普通 APN、移动数据、程序解析、SIM 存储或 Telegram。** 手机关闭数据漫游仍能收，模块原始串口又没有任何字节，下游和普通 Internet APN 都没有可处理或可修复的环节。
5. **极低：携号转网或 CTExcel 账户状态。** 同一张 SIM 在手机中通过 VoLTE 收取正常，无法解释只在当前模组中失败。

## 推荐的下一步

### 1. 向板卡卖家或 SIMCom 获取确认

提供以下非敏感信息：

- 型号：SIM7600CE-T；
- 固件：`LE20B04SIM7600M22_221130`；
- 场景：英国 CTExcel SIM 在中国大陆漫游，自动驻留中国电信 LTE；
- 注册：`CEREG=5`、`CGREG=5`、`CREG=3`；
- 手机验证：同样驻留中国电信；关闭 Wi-Fi/数据漫游仍能收，关闭 VoLTE 后收不到，重开 VoLTE 后延迟补投递；
- 模块表现：入站无 `+CMTI/+CMT`，出站无 `+CMGS`。

需要厂商明确回答：

1. 该固件是否支持国际漫游 SIM 在中国电信 LTE 上完成 IMS 注册并收发 SMS over IMS？
2. 当前固件用哪个受支持命令查看 IMS/VoLTE/SMS 注册状态？
3. 是否存在匹配 SIM7600CE-T 的固件或 MBN/QCN 运营商配置？
4. 如果升级，校验值、升级工具、前置条件和回退方法是什么？
5. 是否能提供“IMS registered + 实际出现 `+CMTI/+CMT`/`+CMGS`”的验收方法，而不是只验证 `CEREG=5` 或能联网？

### 2. 向 CTExcel 确认漫游承载

现场已确认手机实际访问中国电信，且短信对 VoLTE 开关有决定性依赖。向 CTExcel 询问时，应要求正式确认中国大陆漫游短信是否为 SMS over IMS，以及是否对普通手机以外的 LTE 模组、IMEI/TAC 类型、IMS profile 或终端认证有限制。沟通时不要把号码、验证码或账户凭据写入本项目文档。

### 3. 做一次决定性的 A/B 测试

优先使用一款厂商明确支持中国电信 VoLTE/IMS SMS、并开放 IMS 注册状态查询的模组，保持同一张 SIM、同一地点和同一入站验证码来源。验收必须同时看到 IMS 已注册以及实际 `+CMTI/+CMT`（入站）或 `+CMGS`（出站）；仅能联网、显示 VoLTE 能力或 `CEREG=5` 均不算通过。

### 4. 后续代理接手清单

1. 先通读本文，不重复已经失败的付费短信测试。
2. 只读查询 `/api/status`，重新记录连接、信号和三个注册域；不要输出 `own_number`、余额或配置文件内容。
3. 确认运行实例占用串口时，不再用第二个程序打开 COM 口。
4. 如需新一轮入站测试，先记录 API/存档基线，再由用户触发一条验证码并等待 2 分钟。
5. 只有获得用户明确授权和可靠回退方案后，才考虑固件、运营商 profile 或硬件配置变更。
